// Modules
import React from 'react';
import styled from 'styled-components';
// Components

// Styling
const StyledComponent = styled.div`
	// Size of self

	// Positioning of self

	// Positioning of children

	// Looks of self

	// Looks of children
	
`;

// Template Component - this is the template file for new components
const Template = () => (
	<StyledComponent>
		<h1>Template</h1>
	</StyledComponent>
)

export default Template;