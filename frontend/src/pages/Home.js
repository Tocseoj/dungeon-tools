// Modules
import React from 'react';
import styled from 'styled-components';
// Components

// Styling
const StyledComponent = styled.div`
	// Size of self

	// Positioning of self

	// Positioning of children

	// Looks of self

	// Looks of children

`;

// Home Page - this is loaded on the '/' path
const Home = () => (
	<StyledComponent>
		<h1>Home</h1>
	</StyledComponent>
)

export default Home;